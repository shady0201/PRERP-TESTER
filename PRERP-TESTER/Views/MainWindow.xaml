<Window x:Class="PRERP_TESTER.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:PRERP_TESTER.ViewModels"
        xmlns:views="clr-namespace:PRERP_TESTER.Views"
        Title="PRERP TESTER - Automation System" 
        Height="768" Width="1366"
        WindowStartupLocation="CenterScreen">
    <Grid Background="White">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="201"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        
        <!-- Menu Left -->
        <Border Grid.Column="0" BorderBrush="#FFCCCCCC" BorderThickness="0,0,1,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="235*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <StackPanel Grid.Row="0">
                    <TextBlock Text="PRERP TESTER" Foreground="#FF353535" FontSize="11" FontWeight="SemiBold"/>
                </StackPanel>
                <!-- Danh sách module -->
                <ListBox Grid.Row="1"
                        ItemsSource="{Binding Modules}"
                        SelectedItem="{Binding SelectedModule}"
                        ItemContainerStyle="{StaticResource MenuItemStyle}"
                        Background="Transparent"
                        BorderThickness="0"
                        ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                        ScrollViewer.VerticalScrollBarVisibility="Auto"
                        Padding="5,0,5,0"
                        VerticalAlignment="Stretch">

                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <ContentControl Content="{Binding}"
                                            ContentTemplate="{StaticResource MenuItem}" />
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
                
            </Grid>
        </Border>

        <!-- Content Right -->
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="187*"/>
            </Grid.RowDefinitions>
            <ContentControl Grid.Row="0"
                        HorizontalContentAlignment="Stretch" 
                        VerticalContentAlignment="Stretch"
                        Content="{Binding SelectedModule}" Grid.RowSpan="2"/>
        </Grid>
    </Grid>
</Window>