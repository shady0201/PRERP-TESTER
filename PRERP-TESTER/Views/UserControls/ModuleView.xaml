<UserControl    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
                xmlns:av="http://schemas.microsoft.com/expression/blend/2008"
                xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                xmlns:conv="clr-namespace:PRERP_TESTER.Converters"
                xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
                xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
                xmlns:ei="http://schemas.microsoft.com/xaml/behaviors"
                mc:Ignorable="av"
                x:Class="PRERP_TESTER.Views.UserControls.ModuleView"
                av:DesignHeight="346.928" av:DesignWidth="754.237">

    <UserControl.Resources>
        <conv:SubtractValueConverter x:Key="SubtractValueConverter" />
    </UserControl.Resources>

    <Border Padding="6" Background="White">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Margin="0,0,0,4">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                            
                    <Grid Grid.Column="0" VerticalAlignment="Center">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="10">
                            <iconPacks:PackIconMaterial Kind="Folder" 
                                                        Width="20" Height="20"
                                                        Grid.Column="0"
                                                        Margin="0,0,6,0"
                                                        Foreground="#FFE2A862"
                                                        HorizontalAlignment="Center"
                                                        VerticalAlignment="Center"/>
                            <TextBlock Text="{Binding Name}" FontSize="25" FontWeight="SemiBold" Foreground="#FF525252"/>
                        </StackPanel>
                    </Grid>
                    
                    <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Command="{Binding AddAccountToModuleCommand}"
                                Content="Thêm Tài Khoản"
                                FontSize="12"
                                Tag="Plus"
                                Style="{StaticResource AddAccountStyle}">
                        </Button>
                    </StackPanel>
                </Grid>
            </Border>
            <!-- Account List-->
            <Border Grid.Row="1"
                    Background="White"
                    BorderBrush="#FFA5A5A5"
                    BorderThickness="0,0,0,1.5"
                    Height="auto"
                    Padding="0,0,0,2"
                    Margin="0,0,0,5">
                <AdornerDecorator>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                    
                        <ListBox ItemsSource="{Binding ModuleAccounts}"
                                 SelectedItem="{Binding SelectedAccountModule}"
                                 ItemContainerStyle="{StaticResource AccountItemContainerStyle}"
                                 Background="Transparent"
                                 MinHeight="40px"
                                 ScrollViewer.HorizontalScrollBarVisibility="Auto"
                                 ScrollViewer.VerticalScrollBarVisibility="Disabled"
                                 MaxWidth="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType=Border}, Converter={StaticResource SubtractValueConverter}, ConverterParameter=0}"
                                 AllowDrop="True"
                                 PreviewMouseMove="ListBox_PreviewMouseMove"
                                 Drop="ListBox_Drop"
                                 DragOver="ListBox_DragOver"
                                 BorderThickness="0">

                            <ListBox.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <StackPanel Orientation="Horizontal" Height="Auto">
                                        <i:Interaction.Behaviors>
                                            <ei:FluidMoveBehavior AppliesTo="Children" Duration="0:0:0.3">
                                                <ei:FluidMoveBehavior.EaseY>
                                                    <CubicEase EasingMode="EaseOut"/>
                                                </ei:FluidMoveBehavior.EaseY>
                                                <ei:FluidMoveBehavior.EaseX>
                                                    <CubicEase EasingMode="EaseOut"/>
                                                </ei:FluidMoveBehavior.EaseX>
                                            </ei:FluidMoveBehavior>
                                        </i:Interaction.Behaviors>
                                    </StackPanel>
                                </ItemsPanelTemplate>
                            </ListBox.ItemsPanel>
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <ContentControl Content="{Binding}" ContentTemplate="{StaticResource AccountItem}" />
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </Grid>
                </AdornerDecorator>
            </Border>
            <!-- web browers-->
            <ItemsControl ItemsSource="{Binding ModuleAccounts}" Grid.Row="2" Grid.ColumnSpan="2">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <Grid />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>

                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Grid Visibility="{Binding IsSelected, Converter={StaticResource BoolToVis}}">
                            <ContentControl Content="{Binding}">
                                <ContentControl.Style>
                                    <Style TargetType="ContentControl">
                                        <Setter Property="ContentTemplate" Value="{x:Null}" />
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsLoaded}" Value="True">
                                                <Setter Property="ContentTemplate">
                                                    <Setter.Value>
                                                        <DataTemplate>
                                                            <ContentPresenter Content="{Binding}" />
                                                        </DataTemplate>
                                                    </Setter.Value>
                                                </Setter>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </ContentControl.Style>
                            </ContentControl>
                        </Grid>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Grid>
    </Border>
</UserControl>