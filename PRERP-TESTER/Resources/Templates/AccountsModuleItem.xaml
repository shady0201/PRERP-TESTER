<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
                    xmlns:ei="http://schemas.microsoft.com/expression/2010/interactions"
                    xmlns:vm="clr-namespace:PRERP_TESTER.ViewModels"
                    xmlns:i="http://schemas.microsoft.com/xaml/behaviors">

    <DataTemplate x:Key="AccountItem">
        <Border Background="Transparent" Padding="0,5" Tag="{Binding DataContext, RelativeSource={RelativeSource AncestorType=ListBox}}">
            <Border.ContextMenu>
                <ContextMenu Style="{StaticResource TabContextMenuStyle}">
                    <MenuItem Header="Thông tin" 
                              Style="{StaticResource TabContextMenuLeftStyle}"
                              Command="{Binding PlacementTarget.Tag.ShowAccountDetailCommand,RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                              CommandParameter="{Binding}">
                        <MenuItem.Icon>
                            <iconPacks:PackIconMaterial Kind="CardAccountDetailsStarOutline" Width="14" Height="14"/>
                        </MenuItem.Icon>
                    </MenuItem>
                    <MenuItem Header="Gỡ tài khoản"
                              Style="{StaticResource TabContextMenuLeftStyle}"
                              Foreground="#EF4444"
                              Command="{Binding PlacementTarget.Tag.RemoveAccountFromModuleCommand,RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                              CommandParameter="{Binding}">
                        <MenuItem.Icon>
                            <iconPacks:PackIconMaterial Kind="DeleteOutline" Width="14" Height="14" Foreground="#EF4444"/>
                        </MenuItem.Icon>
                    </MenuItem>
                </ContextMenu>
            </Border.ContextMenu>
            <Grid Margin="3,0,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <!--Avatar-->
                <Border Grid.Column="0" Width="30" Height="30" Margin="5,0,0,0" VerticalAlignment="Center">
                    <Grid >
                        <Border CornerRadius="6" 
                                Background="{Binding Account.AvatarColor}"
                                BorderThickness="0.5">
                            <TextBlock Text="{Binding Account.Initials}" 
                                        Foreground="White" 
                                        FontSize="14" 
                                        FontWeight="Bold"
                                        VerticalAlignment="Center" 
                                        HorizontalAlignment="Center"
                                        Margin="0,0,0,1"/>
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Account.AvatarUrl}" Value="{x:Null}">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding Account.AvatarUrl}" Value="">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                        </Border>

                        <Border CornerRadius="6" ClipToBounds="True" BorderThickness="1" BorderBrush="#40000000">
                            <Image Source="{Binding Account.AvatarUrl, TargetNullValue={x:Null}, FallbackValue={x:Null}}" 
                                    Stretch="UniformToFill"
                                    RenderOptions.BitmapScalingMode="HighQuality">
                                <Image.Clip>
                                    <RectangleGeometry Rect="0,0,30,30" RadiusX="7" RadiusY="7" />
                                </Image.Clip>
                                <Image.Style>
                                    <Style TargetType="Image">
                                        <Setter Property="Visibility" Value="Visible"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Account.AvatarUrl}" Value="{x:Null}">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Account.AvatarUrl}" Value="">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Image.Style>
                            </Image>
                            <ToolTipService.InitialShowDelay>0</ToolTipService.InitialShowDelay>
                            <ToolTipService.BetweenShowDelay>0</ToolTipService.BetweenShowDelay>
                            <Border.ToolTip>
                                <ToolTip Padding="8,2" HasDropShadow="True"
                                         SnapsToDevicePixels="True" Style="{StaticResource TooltipAccountName}">
                                    <TextBlock Text="{Binding Account.DisplayName}" FontWeight="SemiBold"/>
                                </ToolTip>
                            </Border.ToolTip>
                        </Border>
                    </Grid>
                </Border>
                <!-- Account Name -->
                <TextBlock  Grid.Column="1"
                                    x:Name="AccText"
                                    VerticalAlignment="Center"
                                    Text="{Binding Account.DisplayName}"
                                    FontSize="14"
                                    FontWeight="SemiBold"
                                    TextWrapping="NoWrap"
                                    Margin="6,0,0,0"
                                    MaxWidth="164"
                                    TextTrimming="CharacterEllipsis">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Foreground" Value="{DynamicResource PrimaryText}"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Source={x:Static vm:MainViewModel.Instance}, Path=IsAccountListCollapsed}" Value="True">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </Grid>
        </Border>
    </DataTemplate>

</ResourceDictionary>