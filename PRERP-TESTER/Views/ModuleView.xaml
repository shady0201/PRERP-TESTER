<UserControl    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
                xmlns:av="http://schemas.microsoft.com/expression/blend/2008"
                xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                xmlns:conv="clr-namespace:PRERP_TESTER.Converters"
                xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
                mc:Ignorable="av"
                x:Class="PRERP_TESTER.Views.ModuleView"
                av:DesignHeight="290.913" av:DesignWidth="642.208">

    <UserControl.Resources>
        <conv:SubtractValueConverter x:Key="SubtractValueConverter" />
    </UserControl.Resources>

    <Border Padding="6" Background="#EEECF0">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            
            <Border Grid.Row="0" Margin="0,0,0,4">
                <StackPanel>
                    <TextBlock Text="{Binding Name}" FontSize="18" FontWeight="Bold" Foreground="#333"/>
                </StackPanel>
            </Border>
             <!-- Account List-->
            <Border Grid.Row="1"
                    Background="#FFF8F8F9"
                    BorderBrush="#FFF3F3F3"
                    BorderThickness="0.5"
                    CornerRadius="5"
                    Height="auto"
                    Margin="0,0,0,5">

                <Border.Effect>
                    <DropShadowEffect Color="#FFCECECE" 
                                      Direction="0" 
                                      ShadowDepth="2"
                                      Opacity="0.3"
                                      BlurRadius="7"/>
                </Border.Effect>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <ListBox ItemsSource="{Binding ModuleAccounts}"
                                 SelectedItem="{Binding SelectedAccountModule}"
                                 ItemContainerStyle="{StaticResource AccountTabItemStyle}"
                                 Background="Transparent"
                                 ScrollViewer.HorizontalScrollBarVisibility="Auto"
                                 ScrollViewer.VerticalScrollBarVisibility="Disabled"
                                 MaxWidth="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType=Border}, Converter={StaticResource SubtractValueConverter}, ConverterParameter=40}"
                                 BorderThickness="0">
                        <ListBox.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel Orientation="Horizontal"/>
                            </ItemsPanelTemplate>
                        </ListBox.ItemsPanel>
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <ContentControl Content="{Binding}" ContentTemplate="{StaticResource AccountItem}" />
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                    <Button Grid.Column="1"
                            Command="{Binding AddAccountToModuleCommand}"
                            Width="25" Height="25" Margin="5,8,5,8"
                            Style="{StaticResource AddTabButtonStyle}">
                        <iconPacks:PackIconMaterial Kind="Plus" Width="14" Height="14" VerticalAlignment="Center"/>
                    </Button>
                </Grid>
            </Border>
             <!-- web browers-->
            <ItemsControl ItemsSource="{Binding ModuleAccounts}" Grid.Row="2">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <Grid />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>

                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Grid Visibility="{Binding IsSelected, Converter={StaticResource BoolToVis}}">
                            <ContentControl Content="{Binding}">
                                <ContentControl.Style>
                                    <Style TargetType="ContentControl">
                                        <Setter Property="ContentTemplate" Value="{x:Null}" />
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsLoaded}" Value="True">
                                                <Setter Property="ContentTemplate">
                                                    <Setter.Value>
                                                        <DataTemplate>
                                                            <ContentPresenter Content="{Binding}" />
                                                        </DataTemplate>
                                                    </Setter.Value>
                                                </Setter>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </ContentControl.Style>
                            </ContentControl>
                        </Grid>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Grid>
    </Border>
</UserControl>