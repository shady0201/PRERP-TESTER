<UserControl x:Class="PRERP_TESTER.Views.UserControls.AccountDetailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
             mc:Ignorable="d" d:DesignWidth="350"
             Width="600">
    
    <Border Background="White" CornerRadius="12" Padding="20" BorderBrush="#FF868686" BorderThickness="1">
        <StackPanel>
            <Grid Margin="0,0,0,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Ellipse Width="60" Height="60" Stroke="#FFB5B5B5" StrokeThickness="1">
                    <Ellipse.Fill>
                        <ImageBrush ImageSource="{Binding AvatarUrl, TargetNullValue={x:Null}}" Stretch="UniformToFill"/>
                    </Ellipse.Fill>
                </Ellipse>

                <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="15,0,0,0">
                    <TextBlock Text="{Binding FullName}" FontWeight="Bold" FontSize="18" Foreground="#333333"/>
                    <TextBlock Text="{Binding Username}" FontSize="13" Foreground="#888888" Margin="0,2,0,0"/>
                </StackPanel>
            </Grid>

            <Separator Background="#F0F0F0" Margin="0,0,0,15"/>

            <ScrollViewer MaxHeight="500" VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <Border Cursor="Hand" Background="Transparent" Margin="0,0,0,5">
                        <Border.InputBindings>
                            <MouseBinding MouseAction="LeftClick" 
                                          Command="{Binding TogglePermissionCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                                          CommandParameter="SFT"/>
                        </Border.InputBindings>
                        <StackPanel Orientation="Horizontal">
                            <iconPacks:PackIconMaterial VerticalAlignment="Center" Margin="0,0,5,0" Foreground="Gray" Width="12" Height="12">
                                <iconPacks:PackIconMaterial.Style>
                                    <Style TargetType="iconPacks:PackIconMaterial">
                                        <Setter Property="Kind" Value="ChevronRight"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsSftExpanded}" Value="True">
                                                <Setter Property="Kind" Value="ChevronDown"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </iconPacks:PackIconMaterial.Style>
                            </iconPacks:PackIconMaterial>
                            <TextBlock Text="{Binding PermissionsSFT.Length, StringFormat='Permission SFT ({0})'}" FontSize="16" FontWeight="SemiBold" Foreground="#FF5D5D5D"/>
                        </StackPanel>
                    </Border>
                    <ItemsControl ItemsSource="{Binding PermissionsSFT}">
                        <ItemsControl.Style>
                            <Style TargetType="ItemsControl">
                                <Setter Property="Visibility" Value="Collapsed"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsSftExpanded}" Value="True">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </ItemsControl.Style>
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel Orientation="Vertical" Width="Auto"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <Border Grid.Column="0" Background="#E3F2FD" CornerRadius="4" Padding="6,3" Margin="0,2,5,2">
                                        <TextBlock Text="{Binding Name}" FontSize="11" Foreground="#FF123F71"/>
                                    </Border>
                                    <Border Grid.Column="1">
                                        <ItemsControl ItemsSource="{Binding Role}">
                                            <ItemsControl.ItemsPanel>
                                                <ItemsPanelTemplate>
                                                    <StackPanel Orientation="Horizontal" Width="Auto"/>
                                                </ItemsPanelTemplate>
                                            </ItemsControl.ItemsPanel>
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <Border Grid.Column="0" Background="#FFFDF5E3" CornerRadius="4" Padding="6,3" Margin="0,2,5,2">
                                                        <TextBlock Text="{Binding}" FontSize="11" Foreground="#FF9C7C24"/>
                                                    </Border>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </Border>
                                </Grid>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>

                    <Border Cursor="Hand" Background="Transparent" Margin="0,0,0,5">
                        <Border.InputBindings>
                            <MouseBinding MouseAction="LeftClick" Command="{Binding TogglePermissionCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="MASSET"/>
                        </Border.InputBindings>
                        <StackPanel Orientation="Horizontal">
                            <iconPacks:PackIconMaterial VerticalAlignment="Center" Margin="0,0,5,0" Foreground="Gray" Width="12" Height="12">
                                <iconPacks:PackIconMaterial.Style>
                                    <Style TargetType="iconPacks:PackIconMaterial">
                                        <Setter Property="Kind" Value="ChevronRight"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsMassetExpanded}" Value="True">
                                                <Setter Property="Kind" Value="ChevronDown"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </iconPacks:PackIconMaterial.Style>
                            </iconPacks:PackIconMaterial>
                            <TextBlock Text="{Binding PermissionsMASSET.Length, StringFormat='Permission MASSET ({0})'}" FontSize="16" FontWeight="SemiBold" Foreground="#FF5D5D5D"/>
                        </StackPanel>
                    </Border>
                    <ItemsControl ItemsSource="{Binding PermissionsMASSET}">
                        <ItemsControl.Style>
                            <Style TargetType="ItemsControl">
                                <Setter Property="Visibility" Value="Collapsed"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsMassetExpanded}" Value="True">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </ItemsControl.Style>
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel Orientation="Vertical" Width="Auto"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <Border Grid.Column="0" Background="#E3F2FD" CornerRadius="4" Padding="6,3" Margin="0,2,5,2">
                                        <TextBlock Text="{Binding Name}" FontSize="11" Foreground="#FF123F71"/>
                                    </Border>
                                    <Border Grid.Column="1">
                                        <ItemsControl ItemsSource="{Binding Role}">
                                            <ItemsControl.ItemsPanel>
                                                <ItemsPanelTemplate>
                                                    <StackPanel Orientation="Horizontal" Width="Auto"/>
                                                </ItemsPanelTemplate>
                                            </ItemsControl.ItemsPanel>
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <Border Grid.Column="0" Background="#FFFDF5E3" CornerRadius="4" Padding="6,3" Margin="0,2,5,2">
                                                        <TextBlock Text="{Binding}" FontSize="11" Foreground="#FF9C7C24"/>
                                                    </Border>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </Border>
                                </Grid>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>

                    <Border Cursor="Hand" Background="Transparent" Margin="0,0,0,5">
                        <Border.InputBindings>
                            <MouseBinding MouseAction="LeftClick" Command="{Binding TogglePermissionCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="HASSET"/>
                        </Border.InputBindings>
                        <StackPanel Orientation="Horizontal">
                            <iconPacks:PackIconMaterial VerticalAlignment="Center" Margin="0,0,5,0" Foreground="Gray" Width="12" Height="12">
                                <iconPacks:PackIconMaterial.Style>
                                    <Style TargetType="iconPacks:PackIconMaterial">
                                        <Setter Property="Kind" Value="ChevronRight"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsHassetExpanded}" Value="True">
                                                <Setter Property="Kind" Value="ChevronDown"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </iconPacks:PackIconMaterial.Style>
                            </iconPacks:PackIconMaterial>
                            <TextBlock Text="{Binding PermissionsHASSET.Length, StringFormat='Permission HASSET ({0})'}" FontSize="16" FontWeight="SemiBold" Foreground="#FF5D5D5D"/>
                        </StackPanel>
                    </Border>
                    <ItemsControl ItemsSource="{Binding PermissionsHASSET}">
                        <ItemsControl.Style>
                            <Style TargetType="ItemsControl">
                                <Setter Property="Visibility" Value="Collapsed"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsHassetExpanded}" Value="True">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </ItemsControl.Style>
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel Orientation="Vertical" Width="Auto"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <Border Grid.Column="0" Background="#E3F2FD" CornerRadius="4" Padding="6,3" Margin="0,2,5,2">
                                        <TextBlock Text="{Binding Name}" FontSize="11" Foreground="#FF123F71"/>
                                    </Border>
                                    <Border Grid.Column="1">
                                        <ItemsControl ItemsSource="{Binding Role}">
                                            <ItemsControl.ItemsPanel>
                                                <ItemsPanelTemplate>
                                                    <StackPanel Orientation="Horizontal" Width="Auto"/>
                                                </ItemsPanelTemplate>
                                            </ItemsControl.ItemsPanel>
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <Border Grid.Column="0" Background="#FFFDF5E3" CornerRadius="4" Padding="6,3" Margin="0,2,5,2">
                                                        <TextBlock Text="{Binding}" FontSize="11" Foreground="#FF9C7C24"/>
                                                    </Border>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </Border>
                                </Grid>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>

                </StackPanel>
            </ScrollViewer>
        </StackPanel>
    </Border>
</UserControl>
