<UserControl x:Class="PRERP_TESTER.Views.UserControls.AccountDetailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" d:DesignWidth="350"
             Width="600">
    
    <Border Background="White" CornerRadius="12" Padding="20" BorderBrush="#FF868686" BorderThickness="1">
        <StackPanel>
            <Grid Margin="0,0,0,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Ellipse Width="60" Height="60" Stroke="#EEEEEE" StrokeThickness="2">
                    <Ellipse.Fill>
                        <ImageBrush ImageSource="{Binding AvatarUrl, TargetNullValue={x:Null}}" Stretch="UniformToFill"/>
                    </Ellipse.Fill>
                </Ellipse>

                <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="15,0,0,0">
                    <TextBlock Text="{Binding FullName}" FontWeight="Bold" FontSize="18" Foreground="#333333"/>
                    <TextBlock Text="{Binding Username}" FontSize="13" Foreground="#888888" Margin="0,2,0,0"/>
                </StackPanel>
            </Grid>

            <Separator Background="#F0F0F0" Margin="0,0,0,15"/>

            <ScrollViewer MaxHeight="500" VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <TextBlock Text="Permission SFT" FontWeight="SemiBold" Foreground="#FF5D5D5D" Margin="0,5"/>
                    <ItemsControl ItemsSource="{Binding PermissionsSFT}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel Orientation="Vertical" Width="Auto"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <Border Grid.Column="0" Background="#E3F2FD" CornerRadius="4" Padding="6,3" Margin="0,2,5,2">
                                        <TextBlock Text="{Binding Name}" FontSize="11" Foreground="#FF123F71"/>
                                    </Border>
                                    <Border Grid.Column="1">
                                        <ItemsControl ItemsSource="{Binding Role}">
                                            <ItemsControl.ItemsPanel>
                                                <ItemsPanelTemplate>
                                                    <StackPanel Orientation="Horizontal" Width="Auto"/>
                                                </ItemsPanelTemplate>
                                            </ItemsControl.ItemsPanel>
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <Border Grid.Column="0" Background="#FFFDF5E3" CornerRadius="4" Padding="6,3" Margin="0,2,5,2">
                                                        <TextBlock Text="{Binding}" FontSize="11" Foreground="#FF9C7C24"/>
                                                    </Border>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </Border>
                                </Grid>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>

                    <TextBlock Text="Permission MASSET" FontWeight="SemiBold" Foreground="#FF5D5D5D" Margin="0,5"/>
                    <ItemsControl ItemsSource="{Binding PermissionsMASSET}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel Orientation="Vertical" Width="Auto"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <Border Grid.Column="0" Background="#E3F2FD" CornerRadius="4" Padding="6,3" Margin="0,2,5,2">
                                        <TextBlock Text="{Binding Name}" FontSize="11" Foreground="#FF123F71"/>
                                    </Border>
                                    <Border Grid.Column="1">
                                        <ItemsControl ItemsSource="{Binding Role}">
                                            <ItemsControl.ItemsPanel>
                                                <ItemsPanelTemplate>
                                                    <StackPanel Orientation="Horizontal" Width="Auto"/>
                                                </ItemsPanelTemplate>
                                            </ItemsControl.ItemsPanel>
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <Border Grid.Column="0" Background="#FFFDF5E3" CornerRadius="4" Padding="6,3" Margin="0,2,5,2">
                                                        <TextBlock Text="{Binding}" FontSize="11" Foreground="#FF9C7C24"/>
                                                    </Border>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </Border>
                                </Grid>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>

                    <TextBlock Text="Permission HASSET" FontWeight="SemiBold" Foreground="#FF5D5D5D" Margin="0,5"/>
                    <ItemsControl ItemsSource="{Binding PermissionsHASSET}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel Orientation="Vertical" Width="Auto"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <Border Grid.Column="0" Background="#E3F2FD" CornerRadius="4" Padding="6,3" Margin="0,2,5,2">
                                        <TextBlock Text="{Binding Name}" FontSize="11" Foreground="#FF123F71"/>
                                    </Border>
                                    <Border Grid.Column="1">
                                        <ItemsControl ItemsSource="{Binding Role}">
                                            <ItemsControl.ItemsPanel>
                                                <ItemsPanelTemplate>
                                                    <StackPanel Orientation="Horizontal" Width="Auto"/>
                                                </ItemsPanelTemplate>
                                            </ItemsControl.ItemsPanel>
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <Border Grid.Column="0" Background="#FFFDF5E3" CornerRadius="4" Padding="6,3" Margin="0,2,5,2">
                                                        <TextBlock Text="{Binding}" FontSize="11" Foreground="#FF9C7C24"/>
                                                    </Border>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </Border>
                                </Grid>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </ScrollViewer>
        </StackPanel>
    </Border>
</UserControl>
